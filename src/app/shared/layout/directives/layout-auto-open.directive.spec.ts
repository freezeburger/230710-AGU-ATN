import { LayoutAutoOpenDirective } from './layout-auto-open.directive';

describe('LayoutAutoOpenDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutAutoOpenDirective();
    expect(directive).toBeTruthy();
  });
});
