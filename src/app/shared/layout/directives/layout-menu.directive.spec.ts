import { LayoutMenuDirective } from './layout-menu.directive';

describe('LayoutMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutMenuDirective();
    expect(directive).toBeTruthy();
  });
});
