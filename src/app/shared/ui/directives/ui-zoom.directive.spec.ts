import { UiZoomDirective } from './ui-zoom.directive';

describe('UiZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new UiZoomDirective();
    expect(directive).toBeTruthy();
  });
});
